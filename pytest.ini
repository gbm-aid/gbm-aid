; GBM-AID pytest yapilandirmasi (2026-09-13, karar 40)
; =====================================================
;
; Bu dosya 2026-09-13'e kadar YOKTU (ne burada ne proje kokunde; ne
; pytest.ini, ne setup.cfg, ne pyproject.toml, ne tox.ini). Karar 40 ile
; eklendi.
;
; ## NE ICIN VAR (iki is)
;
; 1) ROOTDIR CAPASI. pytest, yapilandirma dosyasi yoksa rootdir'i
;    argumanlardan tahmin eder; bu da testin NEREDEN cagrildigina gore
;    degisir. Bu depoda kod koku iki ayri yoldan gorunur:
;        C:\Users\Barış\Desktop\GBM-AID Prototip\gbm-aid mert
;        X:\                            (subst eslemesi, bkz. K12)
;    Bu dosya kod kokune sabitlendigi icin rootdir her iki cagrimda da
;    ayni yeri gosterir.
;
; 2) `conftest.py`'yi GORUNUR kilmak. Turkce-yol/ITK sorununun asil
;    cozumu yan taraftaki `conftest.py`'dedir (pytest'in gecici dizinini
;    ASCII bir koke tasir; 14 test bu yuzden aylarca kirmiziydi).
;    Yapilandirmayi arayan biri once bu dosyaya bakar -- bu yorum onu
;    dogru yere yonlendirir.
;
; ## NEDEN DAVRANIS DEGISTIREN BIR AYAR YOK (bilincli)
;
; `testpaths`, `addopts`, `norecursedirs` gibi ayarlar BILEREK
; yazilmamistir: tam paket olcumu (994 passed / 17 failed, 2026-09-13
; oncesi) bu ayarlar OLMADAN alinmistir ve karar 40'in kapsami "14 testi
; geri kazan" idi, "toplama davranisini degistir" DEGIL. Toplama kapsamini
; daraltmak/genisletmek ayri bir olcum ve ayri bir karar gerektirir.
;
; DIKKAT: `--basetemp` bu dosyaya EKLENMEMELIDIR. Gerekcesi
; `conftest.py`'nin docstring'inde ("Neden `--basetemp` DEGIL"): sabit bir
; basetemp, pytest'in numarali-dizin + kilit mekanizmasini devre disi
; birakir ve bu depoda ayni anda kosan birden fazla pytest surecinin
; (AKTIF-GOREVLER.md protokolu) birbirinin gecici dosyalarini silmesine
; yol acar.

[pytest]
; 2026-09-16: `api/` ve `pipeline/` `backend/` altina tasindi. Import
; adlari DEGISMEDI (`from pipeline.x import y`); testlerin onlari
; bulabilmesi icin arama yoluna hem kok hem `backend` eklenir.
pythonpath = . backend
