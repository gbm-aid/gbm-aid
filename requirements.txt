annotated-doc==0.0.4
annotated-types==0.8.0
anyio==4.14.2
click==8.4.2
colorama==0.4.6
faiss-cpu==1.15.0
fastapi==0.136.3
h11==0.16.0
httptools==0.8.0
idna==3.18
lifelines==0.30.0
neuroHarmonize==2.5.1
nibabel==5.4.2
numpy==1.26.4
pandas==2.3.3
psycopg2-binary==2.9.12
scikit-learn==1.7.2
shap==0.49.1
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.0.1
PyYAML==6.0.3
redis==7.4.1
scipy==1.15.3
SimpleITK==2.3.1
starlette==1.3.1
statsmodels==0.14.6
typing-inspection==0.4.2
typing_extensions==4.16.0
uvicorn==0.41.0
watchfiles==1.2.0
websockets==16.1.1

# --- Hafta 4 (modeling-agent): XGBoost 12-ay sağkalım modeli.
# EKSİKLİK DÜZELTMESİ (2026-08-28): `pipeline/xgboost_model.py` xgboost'u
# 5 ayrı yerde LAZY (fonksiyon içinde) import ediyor -- bu yüzden pytest
# collection çökmüyor, hata yalnız gerçek çağrıda `ModuleNotFoundError`
# olarak çıkıyordu ve paket HİÇBİR requirements dosyasında pinli DEĞİLDİ.
# Sonuç: CI develop'a merge edilse `test_xgboost_model.py` (40 test) +
# `test_train_xgboost_week4.py` (15 test) patlardı ve koşuların
# reprodüksiyon garantisi yoktu.
# Pinlenen sürüm, 2026-08-28'de İKİ ortamda birden ölçüldü ve aynı çıktı:
# ana Python 3.10 ortamı ve `.venv310_pyradiomics` -> her ikisinde 3.2.0.
xgboost==3.2.0

# --- Hafta 5 (rag-agent, 2026-08-18): PubMed + embedding + geçici FAISS RAG
# indeksi + Upstash REST önbellek istemcisi. `pipeline/rag_*.py`'nin
# doğrudan bağımlılıkları biopython/sentence-transformers/requests'tir;
# torch/transformers/... sentence-transformers'ın transitive bağımlılığı
# olarak otomatik geldi, repronun mevcut `pip freeze` disiplinine uyularak
# ayrıca pinlendi. Model indirmesi: `all-MiniLM-L6-v2` ~88MB (huggingface
# cache), toplam paket indirmesi ~150MB (torch başat, 122MB). `scipy`
# (yukarıda, satır 24) zaten pinliydi -- sentence-transformers'ın da
# transitive bağımlılığı, YENİDEN eklenmedi.
biopython==1.85
sentence-transformers==3.3.1
requests==2.34.2
torch==2.13.0
transformers==4.57.6
huggingface_hub==0.36.2
tokenizers==0.22.2
safetensors==0.8.0
regex==2026.7.19
networkx==3.4.2
sympy==1.14.0
mpmath==1.3.0
filelock==3.32.3
Jinja2==3.1.6
MarkupSafe==3.0.3
urllib3==2.7.0
certifi==2026.7.22
charset-normalizer==3.5.1
fsspec==2026.7.0
tqdm==4.70.0
pillow==12.3.0

# --- Hafta 5 (rag-agent) LLM saglayici SDK'si.
# EKSIKLIK DUZELTMESI (2026-09-17): `pipeline/llm_provider.py:478`
# `openai`'yi LAZY (fonksiyon icinde) import eder -- bu yuzden ne pytest
# collection'i ne de herhangi bir test patliyordu, ve paket HICBIR
# requirements dosyasinda PINLI DEGILDI. Gelistirme makinesinde elle
# kurulu oldugu icin yerelde calisiyor, SUNUCUDA CALISMIYORDU.
# OLCULEN SONUC (canli sunucu https://gbm-aid.tech, 2026-09-17):
# `POST /analyze_patient` literatur blogu PubMed kaynaklarini (7 kaynak,
# Upstash onbelleginden) donduruyor ama `summary_tr` NULL; `llm_reason`
# alani birebir soyle diyordu:
#     "LLM cagrisi basarisiz: LLMProviderError: 'openai' paketi kurulu
#      degil. Kurulum: pip install openai"
# Yapilandirma zinciri (GBMAID_LLM_ENABLED -> OPENAI_API_KEY -> saglayici/
# model) SUNUCUDA ZATEN GECERLIYDI: `llm_provider.py` once onlari dogrular
# (`:278` anahtar kontrolu), paket import'u EN SON adimdir -- yani bu hata
# metnini almak, onceki uc kontrolun GECTIGININ kanitidir.
# Bu bosluk 2026-08-28'deki `xgboost` bosluguyla AYNI SINIFTIR (bkz.
# yukaridaki not): lazy import -> sessiz eksiklik -> yalniz gercek cagrida
# patlama.
# Pinlenen surum gelistirme makinesinde olculdu (importlib.metadata): 3.13.0
openai==3.13.0
